package airlang.test;

public sint a,b;

@buildin
public const sint[,,] arrblk(const uint cols);

@buildin
public const sint[,,]* arrblkptr(const uint cols);

@buildin
public const sint[][] arrjag(const uint cols);

@buildin
public const sint[][]* arrjagptr(const uint cols);

protected uint a[8]={
    0:10,
    1:11,
    2:12,
    3:13,
    4:14,
    5:15,
    6:16,
    7:17
};

uint exp = 10 + 12 * 15;


public sint main(const string[] args){
    int32 a = 0, b = 3;
    a = a + b * a;
    return a + b;
}